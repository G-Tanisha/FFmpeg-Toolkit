# Environment variables configuration template

# Comma-separated list of origins allowed to access the API (for CORS)
ALLOWED_ORIGINS=http://localhost:5173,https://f-fmpeg-toolkit.vercel.app

# Optional: Server Port configuration (default: 8000 for local development)
PORT=8000
